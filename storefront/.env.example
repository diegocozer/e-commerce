# Backend Laravel para o proxy do Vite (/api e /sanctum). Padrão: http://localhost:8000
VITE_API_PROXY_TARGET=http://localhost:8000
# true = roda a loja sem backend, com MSW no navegador (dados de docs/API.md §4)
VITE_USE_MOCKS=false
